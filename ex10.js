const calculaMediaDe = (num1, num2) => {

    num1 = Number (prompt ("Digite o primeiro número:"));
    num2 = Number (prompt ("Digite o segundo número:"));

    return (num1 + num2) / 2;
} 
console.log ("MÉDIA:", calculaMediaDe());